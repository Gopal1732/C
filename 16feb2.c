//reverse of a string

#include<stdio.h>
#include<string.h> 

int main(){
    int size,i;
  char arr[50],ch[50];
  

   size=strlen(ch); 
  printf("%d",size);

printf("enter character:");
  for(int i=0;i<size;i++){
    arr[i]==ch;
  }

  printf("%c",arr[i]);
  
  
  

  return 0;
}